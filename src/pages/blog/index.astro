---
import MainLayout from "../../layouts/MainLayout.astro";
import Header from "../../components/Header";
import BlogList from "../../components/BlogList";
import { en } from "../../i18n/en";

// We'll use EN for now as per the index page pattern
const t = en;
---

<MainLayout t={t} title={`Blog - ${t.blog.title}`}>
  <Header t={t} currentLang="en" client:load />
  
  <main class="pt-24 min-h-screen bg-gray-50/30">
    {/* Hero Section */}
    <section class="bg-white border-b border-gray-100 py-16 md:py-24">
      <div class="container mx-auto px-4 text-center">
        <label class="inline-block px-3 py-1 bg-christmas-red/10 text-christmas-red text-xs font-bold uppercase tracking-widest rounded-full mb-4">
          Our Journal
        </label>
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-black text-gray-900 mb-6">
          {t.blog.title}
        </h1>
        <p class="text-lg md:text-xl text-gray-600 max-w-2xl mx-auto">
          {t.blog.subtitle}
        </p>
      </div>
    </section>

    <BlogList t={t} currentLang="en" client:load />
  </main>
</MainLayout>
